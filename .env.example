# MongoDB connection string (required for backend)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/daily-schedule?retryWrites=true&w=majority

# Optional: API URL for frontend (defaults to /api in production)
# VITE_API_URL=http://localhost:5000/api
